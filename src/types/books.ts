export default interface Book {
    isbn: string;
    title: string;
    author: string;
    publisher: string;
    edition: number;
    pages: number;
    language: string;
    publicationDate: string;
    coverPath: string;
}